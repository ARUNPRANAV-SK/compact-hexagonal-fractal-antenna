import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Compact Hexagonal Fractal Antenna - 2.4 GHz RF Engineering Showcase",
  description:
    "Explore the engineering showcase of the Compact Hexagonal Fractal Patch Antenna operating at 2.4 GHz for Wi-Fi, Bluetooth, and IoT. Designed and simulated by Arun Pranav SK using ANSYS HFSS.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
